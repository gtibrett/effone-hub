// PostCSS config for Tailwind v4 under Next 16 (webpack). Picked up
// automatically by Next.js when present at the project root.
const config = {
	plugins: {
		'@tailwindcss/postcss': {}
	}
};

export default config;
